class Car {
    constructor(x, y, speed, sizeX, sizeY, rotationAngle) {
        this.x = x
        this.y = y
        this.speed = speed
        this.sizeX = sizeX
        this.sizeY = sizeY
        this.rotationAngle = rotationAngle
    }
}
